name: isen_ouest_companion
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ">=2.17.6 <3.0.0"

dependencies:
    flutter:
        sdk: flutter

    intl: ^0.17.0
    hive: ^2.2.3
    hive_flutter: ^1.1.0
    cryptography: ^2.0.5
    cryptography_flutter: ^2.0.2
    isen_aurion_client: ^1.0.4
    flutter_svg: ^1.1.4
    requests: ^4.5.0
    fluttertoast: ^8.0.9

    # personal packages
    progress_hud:
        path: packages/progress_hud
    route_creator:
        path: packages/route_creator

dev_dependencies:
    flutter_test:
        sdk: flutter

    flutter_lints: ^2.0.0
    hive_generator: ^1.1.3
    build_runner: ^2.2.0

flutter:
    uses-material-design: true

    assets:
        - assets/images/logo_hat.svg
